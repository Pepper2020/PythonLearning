import argparse
import shutil

def main():
    parser = argparse.AargumentParser(prog="Batch Renamer", description="Locate specific files within a designated folder and rename them.")
    #parser.add_argument("folder path", type=str, )